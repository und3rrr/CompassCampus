# QR Scanner - Полная документация

## Обзор

QR Scanner - это функция, которая позволяет пользователям сканировать QR коды для быстрой навигации на карте кампуса. Администраторы создают QR коды, привязанные к определённым помещениям, а пользователи сканируют эти коды для быстрого получения информации о местоположении и построения маршрутов.

## Архитектура

### Компоненты системы

1. **AdminScreen** - Управление QR кодами (создание, удаление)
2. **QRCodeService** - Сервис для сохранения и извлечения QR кодов
3. **QRScannerScreen** - UI экран для сканирования QR кодов
4. **MapScreen** - Отображение найденного местоположения и маршрутов

### Поток данных

```
AdminScreen (Create QR)
    ↓
QRCodeService.create_qr_mapping()
    ↓
~/.qr_codes/qr_mappings.json (сохранение)
    ↓
HomeScreen (QR Button)
    ↓
QRScannerScreen (Scan/Manual Input)
    ↓
QRCodeService.get_location_by_qr()
    ↓
MapScreen (Auto-navigate to location)
    ↓
MapWidget (Display on map)
```

## Основные компоненты

### 1. QRCodeService (`services/qr_service.py`)

Сервис для управления QR кодами и их привязкой к местоположениям.

#### Основные методы:

**`create_qr_mapping(node_id, node_name, floor, created_by, description=None)`**
- Создаёт новый QR код и сохраняет привязку к помещению
- Возвращает строку с QR кодом
- Автоматически генерирует UUID для уникальности

**`get_location_by_qr(qr_code)`**
- Получает информацию о местоположении по QR коду
- Возвращает объект `QRCodeMapping` или `None`
- Структура возвращаемого объекта:
  - `qr_code`: строка с кодом
  - `node_id`: ID помещения
  - `node_name`: название помещения
  - `floor`: номер этажа
  - `created_at`: дата создания
  - `created_by`: администратор создавший код
  - `active`: статус активности

**`delete_qr_mapping(qr_code)`**
- Удаляет QR код из системы
- Устанавливает флаг `active=False`

**`list_qr_mappings()`**
- Возвращает список всех активных QR кодов

#### Сохранение данных:

Данные сохраняются в JSON файл:
```
~/.qr_codes/qr_mappings.json
```

Структура файла:
```json
{
  "qr-code-string": {
    "qr_code": "qr-code-string",
    "node_id": "101",
    "node_name": "Аудитория 101",
    "floor": 1,
    "created_at": "2024-01-08T12:34:56.789012",
    "created_by": "admin_id",
    "description": "QR на двери",
    "active": true
  }
}
```

### 2. QRScannerScreen (`screens/qr_scanner_screen.py`)

Экран приложения для сканирования QR кодов.

#### Функциональность:

- **Сканирование камерой** - использует встроенную камеру устройства (если доступна)
- **Ручной ввод** - fallback на TextInput для систем без камеры
- **Распознавание кода** - обработка отсканированного кода
- **Автоматическая навигация** - переход на MapScreen при успешном распознавании
- **Сообщения об ошибках** - информирование пользователя через popups

#### Ключевые методы:

**`_process_qr_code(qr_code)`**
- Основной метод обработки QR кода
- Ищет привязку в QRCodeService
- При успехе вызывает `_navigate_to_map()`
- При ошибке показывает сообщение об ошибке

**`on_manual_scan()`**
- Обработчик ручного ввода QR кода
- Вызывается при нажатии кнопки "Распознать"

**`_navigate_to_map()`**
- Перенаправляет на MapScreen с установленным конечным узлом

**`on_enter()` / `on_leave()`**
- Управление жизненным циклом камеры
- Включение камеры при входе на экран
- Отключение камеры при выходе со экрана

#### Поддержка платформ:

- **Android/iOS** - работает с native камерой
- **Windows/Linux/macOS** - fallback на TextInput при отсутствии камеры
- **Web** - TextInput fallback

### 3. MapScreen интеграция (`screens/map_screen.py`)

Метод `set_end_node_from_qr()` добавлен для интеграции с QR сканером.

#### Параметры:

- `node_id` (str) - ID помещения
- `node_name` (str) - название помещения
- `floor` (int) - номер этажа

#### Логика:

1. Загружает здание из кэша
2. Ищет узел по ID
3. Устанавливает конечный узел (end_node)
4. Переводит на нужный этаж (floor_spinner)
5. Если стартовая точка уже выбрана - строит маршрут
6. Если стартовой точки нет - выбирает первый узел как старт
7. Обновляет информационный текст на карте

### 4. HomeScreen кнопки

Добавлена зелёная кнопка "QR" (0.0, 0.7, 0.3, 1.0) на HomeScreen.

```
┌──────────────────────────────────────┐
│  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐ │
│  │ Map │  │Search│ │Hist │ │Admin │ │
│  └─────┘  └─────┘  └─────┘  └─────┘ │
│  ┌──────┐                             │
│  │  QR  │ (зелёная кнопка)            │
│  └──────┘                             │
└──────────────────────────────────────┘
```

При нажатии: переходит на QRScannerScreen

## Использование

### Для администраторов

1. **Создание QR кода:**
   - Перейти на AdminScreen
   - Нажать кнопку "QR Коды"
   - Заполнить форму:
     - ID узла (например "101")
     - Название (например "Аудитория 101")
     - Этаж (например "1")
   - Нажать "Создать QR код"
   - Скопировать сгенерированный код

2. **Сохранение QR кода:**
   - Распечатать QR код
   - Наклеить на дверь помещения
   - QR код автоматически сохранится в системе

### Для пользователей

1. **Сканирование QR кода:**
   - На HomeScreen нажать кнопку "QR"
   - На QRScannerScreen:
     - Если доступна камера: навести камеру на QR код
     - Если нет камеры: ввести QR код вручную и нажать "Распознать"

2. **Получение информации:**
   - При успешном сканировании приложение покажет название помещения и этаж
   - Автоматически перейдёт на MapScreen

3. **Навигация:**
   - На MapScreen появится маршрут до найденного помещения
   - Можно видеть путь и дистанцию

## Тестирование

### Модульные тесты

Для запуска тестов:

```bash
pytest mobile_app/tests/test_qr_scanner.py -v
```

Тесты проверяют:
- ✅ Создание QR кода
- ✅ Получение локации по QR коду
- ✅ Обработка неизвестного QR кода
- ✅ Работа с несколькими QR кодами
- ✅ Сохранение и загрузка данных

### Интеграционное тестирование

Смотрите [TEST_QR_SCANNER.md](../TEST_QR_SCANNER.md) для пошаговой инструкции ручного тестирования.

## Обработка ошибок

### Ошибки при сканировании

| Ошибка | Решение |
|--------|--------|
| Камера не доступна | Используется TextInput fallback |
| QR код не найден | Показывается сообщение об ошибке |
| API недоступен | Используются локальные данные |
| Узел не найден в базе | Показывается ошибка в MapScreen |

### Логирование

Все операции логируются в `logging`:

```python
logger.info(f"QR code scanned: {mapping.node_name}")
logger.warning(f"Unknown QR code: {qr_code}")
logger.error(f"Error processing QR code: {e}")
```

Логи сохраняются в `~/.kivy/logs/`

## Безопасность

### Текущая реализация

- Коды сохраняются в JSON файл в домашней директории
- Нет шифрования данных (для разработки)
- Нет ограничения на количество кодов

### Рекомендации для продакшена

- 🔒 Добавить шифрование файла `qr_mappings.json`
- 🔒 Реализовать двухфакторную аутентификацию для администраторов
- 🔒 Добавить логирование всех операций с QR кодами
- 🔒 Добавить rate limiting на API методы
- 🔒 Использовать QR коды с контрольными суммами

## Развитие функции

### Планируемые улучшения

1. **Сканирование из галереи** - загрузить QR код из фото
2. **История сканирований** - отслеживать какие помещения искал пользователь
3. **Статистика** - аналитика популярных помещений
4. **QR коды по маршрутам** - серия кодов вдоль маршрута
5. **Веб-версия** - сканирование через веб-интерфейс
6. **Bluetooth маячки** - интеграция с BLE beacon'ами вместо QR

## FAQ

**Можно ли отсканировать один QR код несколько раз?**
Да, один QR код можно сканировать бесконечное количество раз.

**Что происходит, если удалить помещение из системы?**
QR коды останутся в системе, но при сканировании будет ошибка "помещение не найдено".

**Как сбросить все QR коды?**
Удалить файл `~/.qr_codes/qr_mappings.json` и перезапустить приложение.

**Работает ли QR Scanner на iOS?**
Да, работает с native Camera на iOS. На Android требуется установленное приложение с Camera permission.

## Файлы проекта

```
mobile_app/
├── screens/
│   ├── qr_scanner_screen.py      # QR сканер UI
│   └── map_screen.py             # Интеграция с картой
├── services/
│   └── qr_service.py             # Сервис QR кодов
├── tests/
│   └── test_qr_scanner.py        # Модульные тесты
├── main.py                       # Регистрация экрана
└── home_screen.py                # Кнопка QR
```

## Контакты и поддержка

Для вопросов и предложений по QR Scanner функции обратитесь к команде разработки или откройте issue на GitHub.

---

**Версия:** 1.0  
**Дата:** 2024-01-08  
**Статус:** ✅ Завершено
