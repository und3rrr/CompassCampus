# 🚀 Quick Start Guide - Phase 5 New Features

## 🎯 Быстрый старт с новыми функциями

### Запуск приложения

```bash
cd mobile_app
python main.py
```

---

## 👥 Системы авторизации

### Вход как Гост
```
1. Нажать кнопку "Войти как гост"
2. Попадёте на экран карты без сохранения истории
3. Можете просматривать карту и строить маршруты
```

### Вход как Студент
```
1. Выбрать вкладку "Студент"
2. Заполнить форму:
   - Имя: "Иван Петров"
   - Email: "ivan@university.edu"
   - ID: "STU123456"
3. Нажать "Вход"
4. История посещений будет автоматически сохраняться
```

### Вход как Администратор
```
1. Выбрать вкладку "Администратор"
2. Заполнить форму:
   - Имя: "Администратор"
   - Email: "admin@university.edu"
   - ID: "ADM001"
   - Пароль: любой пароль (на этапе разработки)
3. Нажать "Вход"
4. Открывается доступ ко всем функциям редактирования
```

---

## 📍 Программное использование новых сервисов

### 1. Система авторизации

```python
from services.auth_service import AuthenticationService, UserPermission

# Создать сервис
auth = AuthenticationService()

# Вход как студент
student = auth.login_student(
    username="Иван",
    email="ivan@uni.edu",
    student_id="123456"
)

# Добавить посещение узла
student.add_visit(
    node_id="101",
    node_name="Аудитория 101",
    floor=1
)

# Проверить право доступа
if auth.has_permission(UserPermission.EDIT_MAP):
    print("Это администратор!")

# Получить текущего пользователя
user = auth.get_current_user()
print(f"Пользователь: {user.username}")
print(f"История: {user.visit_history}")

# Выход
auth.logout()
```

---

### 2. QR Коды

```python
from services.qr_service import QRCodeService

qr = QRCodeService()

# Администратор создаёт QR код для аудитории
qr_code = qr.create_qr_mapping(
    node_id="101",
    node_name="Аудитория 101",
    floor=1,
    created_by="admin_1",
    description="Главная аудитория"
)
print(f"QR код создан: {qr_code}")  # CC-101-a1b2c3d4

# Студент сканирует QR код (в реальности через камеру)
location = qr.get_location_by_qr("CC-101-a1b2c3d4")
print(f"Вы находитесь в: {location.node_name}")

# Получить все QR коды
all_qr = qr.get_all_qr_codes(active_only=True)

# Деактивировать QR код
qr.deactivate_qr_mapping("CC-101-a1b2c3d4")
```

---

### 3. Управление закрытыми маршрутами

```python
from services.route_closure_service import (
    RouteClosureService,
    ClosureType
)
from datetime import datetime, timedelta

closure = RouteClosureService()

# Администратор закрывает коридор между двумя аудиториями
closure_id = closure.close_route(
    from_id="101",
    to_id="102",
    closure_type=ClosureType.REPAIR,
    reason="Ремонт потолка",
    created_by="admin_1",
    scheduled_until=datetime.now() + timedelta(hours=24),
    description="Не проходить здесь сегодня"
)
print(f"Маршрут закрыт: {closure_id}")

# Проверить закрыт ли маршрут
if closure.is_route_closed("101", "102"):
    reason = closure.get_closure_reason("101", "102")
    print(f"Маршрут закрыт. Причина: {reason}")

# Закрыть целую аудиторию
closure.close_node(
    node_id="201",
    closure_type=ClosureType.CLEANING,
    reason="Генеральная уборка",
    created_by="admin_1",
    scheduled_until=datetime.now() + timedelta(hours=4)
)

# Получить список активных закрытий
active = closure.get_active_closures()
for c in active:
    print(f"{c.from_id} -> {c.to_id}: {c.reason}")

# Открыть маршрут
closure.open_route(closure_id)
```

---

### 4. Построение графа из CSV

```python
from services.graph_builder import GraphBuilder

# Узлы из CSV (пример)
nodes = [
    {
        'Id': 15,
        'Name': 'Лестница',
        'Floor': 1,
        'Type': 'Staircase',
        'X': 1135,
        'Y': 553
    },
    {
        'Id': 22,
        'Name': 'Спортзал',
        'Floor': 1,
        'Type': 'Room',
        'X': 809,
        'Y': 112
    },
    # ... остальные узлы
]

# Построить рёбра на основе близости
edges = GraphBuilder.build_edges_from_nodes(nodes)
print(f"Построено {len(edges)} рёбер")

# Конвертировать в список смежности для алгоритма Дейкстры
adjacency = GraphBuilder.edges_to_adjacency_list(edges)
# adjacency['15'] = [('22', 120.5), ...]

# Использовать в расчёте маршрута
def dijkstra(start_id, end_id):
    # Использовать adjacency список
    pass
```

---

## 📊 Структура данных в JSON файлах

### Профиль пользователя (`.profiles/student_123.json`)

```json
{
  "user_id": "student_123",
  "username": "Иван Петров",
  "role": "student",
  "email": "ivan@university.edu",
  "visit_history": [
    {
      "node_id": "101",
      "node_name": "Аудитория 101",
      "floor": 1,
      "timestamp": "2025-12-25T10:30:00",
      "duration_seconds": 3600
    }
  ],
  "last_location": "101",
  "last_location_time": "2025-12-25T11:00:00",
  "qr_codes": {},
  "closed_routes": [],
  "created_at": "2025-12-01T09:00:00",
  "last_login": "2025-12-25T10:00:00"
}
```

---

## 🔍 Отладка и логирование

```python
import logging

# Все сервисы используют логирование
logger = logging.getLogger(__name__)
logger.info("Сообщение")
logger.error("Ошибка")

# Логи появятся в консоли и в `.kivy/logs/`
```

---

## ⚠️ Известные ограничения текущей версии

1. **Сканирование QR** требует интеграции с камерой (нужны `pyzbar` + `opencv`)
2. **Редактирование карты** ещё не реализовано в UI
3. **Администраторский экран** требует создания (AdminScreen)
4. **Экран истории** требует создания (HistoryScreen)
5. **Пароли** не хешируются (демо версия)

---

## 🐛 Решение проблем

### Ошибка: "File not found: .profiles"
```
Это нормально! Папка создастся автоматически при первом входе.
```

### Ошибка: "Cannot import module"
```bash
# Убедитесь, что установили все зависимости
pip install -r requirements.txt
```

### Приложение закрывается при входе
```
Проверьте логи в ~/.kivy/logs/
Убедитесь, что используете Python 3.10+
```

---

## 📱 Сборка для Android

```bash
# Обновить buildozer.spec
vim buildozer.spec

# Добавить разрешения
android.permissions = CAMERA,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE

# Собрать APK
buildozer android debug

# Установить на устройство
adb install bin/*.apk
```

---

## 🎓 Примеры использования в реальном коде

### Пример 1: Сохранить посещение узла

```python
# В main.py или в экране
from services.auth_service import get_auth_service

auth = get_auth_service()
if auth.get_current_user():
    user = auth.get_current_user()
    user.add_visit("101", "Аудитория 101", 1)
    auth._save_profile(user)
```

### Пример 2: Отмечать закрытые маршруты красным

```python
# В map_widget.py при отрисовке маршрута
from services.route_closure_service import get_closure_service

closure_service = get_closure_service()
closed_edges = closure_service.get_closed_edges()

for from_id, to_id in closed_edges:
    # Отрисовать красной линией вместо синей
    canvas.draw_line(..., color=(1, 0, 0, 1))  # красный
```

### Пример 3: Создать экран администратора

```python
# admin_screen.py
from kivy.uix.screenmanager import Screen
from services.auth_service import get_auth_service, UserRole

class AdminScreen(Screen):
    def on_enter(self):
        auth = get_auth_service()
        if auth.get_current_user().role != UserRole.ADMIN:
            self.manager.current = 'home'
            return
        
        # Показать админ-панель
        self.load_closures()
        self.load_qr_codes()
```

---

## ✅ Чек-лист перед запуском

- [ ] Установлены зависимости: `pip install -r requirements.txt`
- [ ] Python версия 3.10+: `python --version`
- [ ] Папка `mobile_app` - текущая директория
- [ ] Нет ошибок при запуске: `python main.py`
- [ ] Экран входа показывается
- [ ] Можно войти как гост или студент
- [ ] История сохраняется

---

## 🚀 Что дальше?

1. Загрузить данные из `cds.csv`
2. Протестировать на реальном устройстве
3. Интегрировать сканирование QR с камерой
4. Создать AdminScreen для управления
5. Создать HistoryScreen для студентов

---

## 📞 Помощь

Если возникают вопросы, обратитесь к:
- **PHASE5_EXTENDED.md** - подробная документация
- **PHASE5_REPORT.md** - отчёт о разработке
- **Исходный код** сервисов с комментариями

---

**Спасибо за использование CampusCompass! 🎉**
